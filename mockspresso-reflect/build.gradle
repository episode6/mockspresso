apply plugin: DeployableJavaProject

dependencies {
  api libs.javax.inject
  api libs.mockspresso.reflect.guava

  testImplementation libs.bundles.test.common
  testImplementation libs.mockito.core
}

